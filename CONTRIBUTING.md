# 协作规范（3 人小组版）

目标：**任何时候，任何一个人都能拿到一份能跑的代码和一份对得上的论文。**

## 1. 分支规则

| 分支 | 用途 | 谁能推 |
| --- | --- | --- |
| `main` | 稳定版，永远能跑 | 只接受 PR 合并 |
| `feat/<姓名>-<模块>` | 个人开发，如 `feat/wang-paper`、`feat/li-q2-ga` | 本人 |
| `fix/<内容>` | 修 bug | 本人 |

```bash
git switch -c feat/li-q2-ga     # 开分支
git add -A
git commit -m "feat(q2): 加入遗传算法求解并输出图2"
git push -u origin feat/li-q2-ga   # 推上去开 PR
```

## 2. 提交信息格式

```
<类型>(<范围>): <做了什么>

类型：feat 新功能 / fix 修 bug / docs 文档 / paper 论文 / data 数据 / refactor 重构
范围：q1 q2 q3 q4 code paper readme
```

例子：`fix(q3): 修正灵敏度分析循环边界`、`paper: 完成摘要初稿`。

一天推 3~5 次，别攒到最后一次性推——出问题无法回退，也没法确认是谁改坏的。

## 3. PR（合并请求）流程

1. 本地跑通：`python code/run_all.py` 无报错。
2. 开 PR，描述里写：改了什么 / 影响哪张图哪个结论 / 怎么验证的。
3. 至少 **1 人 review** 后合并（谁的模块谁 review），合并方式选 Squash。
4. 合并后删掉功能分支。

小改动（改错别字、补注释）可以直接在网页上改并开 PR，不用克隆。

## 4. 每天的节奏

| 时间 | 动作 |
| --- | --- |
| 09:00 | 15 分钟对齐：今天每个人交付什么，写进 `docs/task-board.md` 和 issue |
| 全程 | 一个模型跑通 → 立刻 commit + push，并在 issue 里贴结果图 |
| 22:00 | 15 分钟收尾：更新进度表、关 issue、确认 main 能跑、写好明天第一件事 |

## 5. 三个不能碰的雷

1. **不要在 main 上直接改**——出了错三个人一起停摆。
2. **不要提交数据、大文件、密钥**——仓库会变得又大又慢，且数据可能泄密。
3. **不要手改论文里的图**——图必须来自 `code/`，改参数就重跑，禁止截图、手绘、Excel 手工调。

## 6. 冲突了怎么办

```bash
git pull --rebase origin main     # 拉最新
# 冲突文件里会出现 <<<<<<< ======= >>>>>>>
# 保留两边都需要的内容，不确定就把两个人叫过来一起看
git add <冲突文件>
git rebase --continue
```

拿不准时不要在群里干等——先把你这一份 push 到自己的分支，保证不丢工作。

